# Public, non-secret catalogue endpoint. The application falls back to local
# sanitized fixtures when omitted.
VITE_LOCADORA_PUBLIC_API_URL=https://example.invalid/v1

# Never put add-on manifest URLs or playback credentials in environment files.
# They belong in protected native application storage once that layer exists.
