{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core permissions for the main Locadora window. No shell, filesystem, or generic HTTP plugin is enabled.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
